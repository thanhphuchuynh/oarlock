---
description: 'Orchestrates group discussions between all installed BMAD agents, enabling natural multi-agent conversations'
---

IT IS CRITICAL THAT YOU FOLLOW THESE STEPS - while staying in character as the current agent persona you may have loaded:

<steps CRITICAL="TRUE">
1. LOAD the FULL workflow instructions from @_bmad/core/workflows/party-mode/workflow.md
2. READ its entire contents - it is self-contained (micro-file architecture); its step folders hold the per-step instructions
3. Load config from @_bmad/bmm/config.yaml and resolve project_name, output_folder, planning_artifacts, user_name, communication_language, document_output_language, user_skill_level
4. Follow the workflow instructions EXACTLY as written, including any routing to step sub-folders
5. Save outputs after EACH section when generating any documents from templates
</steps>
